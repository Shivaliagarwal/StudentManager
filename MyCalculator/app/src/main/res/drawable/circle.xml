<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Default state -->
    <item android:state_pressed="false" android:state_focused="false">
        <shape android:shape="oval">
            <solid android:color="#6A6768" /> <!-- Background color of the button -->
            <size
                android:width="20dp"
            android:height="30dp" /> <!-- Height of the circle -->
        </shape>
    </item>

    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="oval">
            <solid android:color="#FFA500" /> <!-- Background color of the button when pressed -->
            <size
                android:width="40dp"
            android:height="40dp" /> <!-- Height of the circle -->
        </shape>
    </item>

</selector>
